# Changesets

Hello and welcome! This directory contains changesets for published packages in the Platformize monorepo.

When submitting a Pull Request that contains user-facing changes, please run:

```bash
npx changeset
```

and follow the interactive prompts to describe your changes.
